id: mybatis-sqli
name: MyBatis SQL注入
severity: HIGH
description: 检测MyBatis中通过变量替换(${...})产生的SQL注入漏洞。请使用#{...}。
patterns:
  - "\\$\\{[^}]+\\}"
sources:
  - "@RequestParam"
  - "@PathVariable"
  - "HttpServletRequest"
  - "request.getParameter"
  - "System.in"
  - "Scanner"

